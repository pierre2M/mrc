{
  "name": "mrc-site",
  "private": true,
  "version": "0.1.0",
  "description": "Site web de présentation et d'expérimentation du MRC v5.4",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "preview": "pnpm --filter web preview",
    "check": "pnpm --filter web check",
    "lint": "pnpm --filter web lint"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
